# The ctags command I use (Personal backup)

## General

```bash
 ctags -R --languages=C,C++ --c++-kinds=+p --fields=+iaS --extras=+q \
  -f ~/.vim/tags \
  /usr/include /usr/include/c++/12 /usr/include/x86_64-linux-gnu
```

## Linux Kernel

```bash
make tags cscope
```
